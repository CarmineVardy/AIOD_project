data_frame -> per l'elaborazione
numpy -> trasformazione finale prima degli addestramenti

color palette: (1°)Magma or (2°)Viridis

Dataset è ribaltato
	campioni sulle righe
	metaboliti sulle colonne

Project pipeline
Pre-processing
    QC assestement (Carmine)
    QC and duplicate removal
    Anomaly detection (Marco - da testare)
        Standard deviation and z-score
        Benchmarking Anomaly Detection Algorithms
        Loadings removal
            ⚠ Due to a limited loadings availability, choose carefully if removing samples could be the best choice
    Normalization (Carmine)
        Total ion count
        PQN (Probabilistic Quotient Normalization)
        Quantile normalization
    Dataset merging (Marco - da testare)
        Dataset merging -> Normalization / Normalization -> dataset merging
        guardare il cambiamento della media, mediana e dei fattori correttivi tra una scelta e l'altra
        Se effettuare il merging, usare i QC
	SUM-PCA (Marco)
    ⚠ Chemical adjustment
    Math adjustment with log transform
    Graphs generation
        PCA score (Carmine)
        Biplot	(Marco)
        Loadings (Carmine)
        Internal variability (Marco)
        z-score (Marco)
    Centering (Carmine)
	Min-Max
	Auto-scaling
	Pareto (nel paper)
    Cleaning Nan samples (Marco)
        Already done in the original datasets

    Random dataset shuffle
    Dataset splitting in training and test sets (Marco)
        Leave One Out
 	Stratified K-fold
		Kennard-stone
		Duplex
		Onion
        Choose splitting ratio between 70-30% or 80-20%
		Splitting with a clustering method (Non ancora - da vadere se c'è tempo)
	⚠ Random split
        Internal normalization in order to de-correlate training and test
	Analyze and choose the most tailored splitting technique with respect to the sample indipendence
        Biological replicates (In this case from same mother) -> same set
        Normalization
        PCA
        Internal variability
        Compare these analysis with the same ones generated on the dataset and determine if the splitting technique is the most suitable one
     
     PLSDA - SIMCA(Carmine)

___________ COSE DA VEDERE __________

Clustering e tSNE
Undersampling e oversampling
Albero decisionale boosting

